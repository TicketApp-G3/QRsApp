version: '3.9'

services:
  app:
    container_name: ticketAppBff
    build: .
    # command: ./scripts/run.sh
    ports:
      - "${APP_PORT:-8080}:8080"
      - "9229:9229" # Debug port
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
    env_file:
      - .env.docker
